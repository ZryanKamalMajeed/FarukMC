@model FarukMC.Models.Anesthetics
@{
    ViewData["Title"] = "Details";
}

    <div class="container">
        <div class="card">
            <h5 class="card-header">Anesthetic's Details</h5>
            <div class="card-body">


                <dl class="row">
                    <dt class="col-sm-4">
                        @Html.DisplayNameFor(model => model.Name)
                    </dt>
                    <dd class="col-sm-6">
                        @Html.DisplayFor(model => model.Name)
                    </dd>
                </dl>


            </div>
            <div class="card-footer">
                <div>
                    <a asp-action="Edit" asp-route-id="@Model.Id">Edit</a> |
                    <a asp-action="Index">Back to List</a>
                </div>
            </div>
        </div>
    </div>